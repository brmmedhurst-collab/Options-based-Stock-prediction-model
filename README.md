# Options-based-Stock-prediction-model
Using a modifiable time range and increment, this code uses theoretical and experimental models to predict based on the options market at the given time the movement of the stock. Using a given increment (default is 3 days) it builds a report at the end showing the model's accuracy.
